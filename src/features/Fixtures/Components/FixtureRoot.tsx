import React from "react";
import GameInfo from "./GameInfo";

const FixturePage = () => {
  return (
    <React.Fragment>
      <GameInfo />
    </React.Fragment>
  );
};

export default FixturePage;
